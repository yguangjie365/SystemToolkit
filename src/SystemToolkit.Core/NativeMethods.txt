// CsWin32 生成清单（Core 产品工程，2026-09-13，落地计划 B7b）
//
// 用途：「进程 ↔ 网络连接归属」——用 GetExtendedTcpTable 读本机 TCP 连接表（每行含 owning PID），
// 与进程名关联后回答"谁在占端口 / 谁在用内网链路"。
// 生成器会连带生成依赖类型（MIB_TCPTABLE_OWNER_PID 等），不必逐条列出。
//
// ⚠️ 本工程是**首个引入 CsWin32 的产品工程**（此前仅测试工程用它钉布局）：
//    它只是**编译期**生成器（PrivateAssets=all），不进下游依赖、不随应用分发；
//    登记见 ADR-002 §5.3 依赖清单 与 NOTICE.md §4.3。
//
// ⚠️ 本批只做 **IPv4 TCP**：IPv6（MIB_TCP6TABLE_OWNER_PID）与 UDP（MIB_UDPTABLE_OWNER_PID）
//    见 TASKS.md 的 OL-B7 备注，按后续批次补。
// ⚠️ 不列 FreeMibTable：GetExtendedTcpTable 的缓冲区由**调用方**分配，没有系统分配的表要释放
//    （FreeMibTable 是给 GetIfTable2 那类 API 用的）。
GetExtendedTcpTable
MIB_TCPTABLE_OWNER_PID
MIB_TCPROW_OWNER_PID
TCP_TABLE_CLASS
ADDRESS_FAMILY

// ── 备份域（B5b-③ 未变文件复用）────────────────────────────────────────────
// 用途：把上一份快照里**未变文件**的副本「硬链接」进本次快照 —— 同卷零拷贝，
// 且旧快照被保留策略删除后数据依然存活（.NET 10 **没有** CreateHardLink，
// 只有 CreateSymbolicLink，而符号链接在旧快照被删后会指向空洞 = 静默丢数据）。
CreateHardLink

// ── 窗口外框（深色主题加固 2026-09-15）─────────────────────────────────────
// 用途：让**非客户区**（标题栏 / 边框）跟随主题明暗。WPF 只管客户区，切到
// Nvidia.Dark 后标题栏仍是系统浅色 —— 这是深色下可见度最高的不一致（ADR-005
// 「两包各自独立达标」在宿主窗口上的缺口）。
// 属性号：DWMWA_USE_IMMERSIVE_DARK_MODE = 20 —— 官方枚举（dwmapi.h 文档页 2026-09-15 实查）
// 与 Win32 metadata **均只有这一个成员**。工程 TFM 为 net10.0-windows（未钉最低平台版本），
// app.manifest 的 supportedOS 只声明 Windows 10/11；更早系统上社区常用的 19（1809–1903）
// 未获官方文档佐证，按 AGENTS §二·五「禁止凭记忆写常量」不写——旧系统调用失败时静默降级
// （标题栏保持系统配色 + 一条 Warn，见 WindowTitleBarTheme）。
DwmSetWindowAttribute
DWMWINDOWATTRIBUTE
